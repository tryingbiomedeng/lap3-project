## Using the project repo

# To clone the repo
```
git clone git@github.com:tryingbiomedeng/lap3-project.git
cd project-repo
git submodule init
git submodule update
```
# To update submodule
```
git submodule update --remote
```
